# Project properties
org.gradle.jvmargs=-Xmx2048m -XX:MaxMetaspaceSize=512m
org.gradle.parallel=true
org.gradle.caching=true

# Signing properties (override via environment variables or ~/.gradle/gradle.properties)
# Uncomment these when publishing to Maven Central
#signing.keyId=
#signing.password=
#signing.secretKeyRingFile=
